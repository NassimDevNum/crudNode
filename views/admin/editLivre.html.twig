{% extends "base.html.twig" %}

{% block titre %}Admin - Modifier le livre{% endblock %}

{% block contenu %}
<div class="container mt-4">
    <h2>🔧 Modification Admin - {{ livre.nom }}</h2>
    <p class="text-muted">Propriétaire : {{ livre.userId.nom }}</p>

    <form method="POST" action="/admin/livres/edit/{{ livre._id }}">
        <div class="row">
            <div class="col-md-6">
                <h3>Informations :</h3>
                <div class="form-group">
                    <label for="titre">Titre</label>
                    <input type="text" class="form-control" id="titre" name="titre" value="{{ livre.nom }}" required>
                </div>
                <div class="form-group">
                    <label for="auteur">Auteur</label>
                    <input type="text" class="form-control" id="auteur" name="auteur" value="{{ livre.auteur }}" required>
                </div>
                <div class="form-group">
                    <label for="pages">Pages</label>
                    <input type="number" class="form-control" id="pages" name="pages" value="{{ livre.pages }}" required>
                </div>
            </div>
            <div class="col-md-6">
                <h3>Résumé :</h3>
                <div class="form-group">
                    <label for="desc">Description</label>
                    <textarea class="form-control" id="desc" name="desc" rows="8">{{ livre.desc }}</textarea>
                </div>
            </div>
        </div>
        
        <div class="mt-3">
            <button type="submit" class="btn btn-success">💾 Enregistrer les modifications</button>
            <a href="/admin" class="btn btn-secondary">Annuler</a>
        </div>
    </form>
</div>
{% endblock %}